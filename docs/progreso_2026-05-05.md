# Progreso 2026-05-05 — Cierre de bitácora QA y consolidación a `main`

## Resumen

Sesión muy productiva con cinco ejes principales:

1. **Limpieza local + verificación de `transfer_job` ARQ e2e**: el último
   handler ARQ que quedaba sin ejercitar contra app real (sólo se había
   inferido del path idéntico a `pipeline_job`) se validó contra el stack
   docker — 2 PNGs al destino `/tmp/transfer_out/batch_1/` en 0.06 s,
   batch terminal en `transferred`.
2. **Consolidación `feature/web` → `main`**: merge `--no-ff` de 279
   commits, sin tag asociado (el desktop no cambió desde v0.1.2 → tag
   sería duplicación; la web SaaS se despliega vía docker compose, no
   por release.yml).
3. **Cierre de la bitácora QA web 2026-04-27**: los 5 hallazgos abiertos
   restantes (#1, #3, #4, #8, #24) implementados, verificados visualmente
   con Playwright contra el stack docker, y commiteados con tests TDD.
4. **Refactor simplify**: revisión por 3 agentes paralelos detectó
   duplicación entre `BatchStateBadge` y el filtro de `BatchListView`,
   firma híbrida de `fetchAll`, mount secuencial y un guard redundante.
   Todo arreglado.
5. **Roadmap acordado para próximas sesiones**: superadmin → cliente
   local web (TWAIN+transfer) → editor scripts web → i18n → smoke test
   extenso → v0.2.0 SaaS → AI Mode.

Rama `main` y `feature/web` sincronizadas con `origin`. HEAD de
`feature/web`: `0e0efe6`.

## 1. Limpieza local

Antes de empezar trabajo serio, limpieza acumulada:

- Borrado del fichero residuo `--db-path` (vacío, restos de un sandbox).
- Commit `d5726be` actualiza `.mcp.json` para que el filesystem MCP
  server apunte a `/media/nicolas/DATA/Tecnomedia/DocScan` en vez del
  path antiguo de FlexiPy.

## 2. `transfer_job` ARQ e2e contra stack docker

Quedaba como pendiente menor en la memoria. Setup:

- App `SmokePipeline` (id=1, tenant SmokeTest) con `transfer_json`:
  `{mode: folder, destination: /tmp/transfer_out, filename_pattern:
  "{batch_id}_{page_index:04d}", create_subdirs: true}`.
- Batch #1 con 2 páginas en estado `transferred` (re-disparable).

Ejecución:

```
POST /api/batches/1/transfer
→ 202 (estado batch sigue transferred mientras corre)
→ Worker ARQ recibe run_transfer_for_batch(batch_id=1)
→ asyncio.to_thread(_transfer_runner, batch_id=1, storage=...) en 0.06 s
→ /tmp/transfer_out/batch_1/{1_0000.png, 1_0001.png}
→ batch terminal en "transferred"
```

Cadena ARQ con `transfer_job` confirmada en producción real. Los tres
handlers (`pipeline_job`, `transfer_job`, `recovery_job`) están ya
verificados contra app real.

## 3. Consolidación `feature/web` → `main`

Antes del merge se verificó con `git diff` que entre `v0.1.2` y HEAD
de `feature/web` no había cambios en el desktop (solo `.mcp.json` que
no entra al instalador). Implicación: tag `v0.2.0` ahora generaría
una release del desktop idéntica a la v0.1.2 — wasteful. Decisión:
**merge sin tag**, la web SaaS se despliega vía docker compose, no por
`release.yml` (que solo construye AppImage + setup.exe).

```
git checkout main
git merge --no-ff feature/web -m "merge: feature/web → main — versión web SaaS DocScan Studio"
git push origin main   # 280 commits avance
```

Commit de merge: `61fdb47`. Rama `feature/web` se queda como rama de
trabajo activa.

## 4. Cierre de bitácora QA — 5 hallazgos

### 4.1 Refinamientos UX (#1, #4, #24) — commit `0e4f248`

Tres quick wins agrupados en un solo commit:

- **#4** `BatchListView`: el subtítulo decía "Todos los lotes del
  inquilino" — traducción literal de "tenant" que en SaaS español queda
  raro. Cambiado a `Todos los lotes de {{ auth.user?.tenant_name }}`,
  consistente con `TeamView`.
- **#1** `TeamView`: el bloque izquierdo del row (avatar+nombre+email)
  ahora se atenúa con `opacity-50` cuando `user.active=false`. Antes el
  badge "Inactivo" estaba a la derecha y no saltaba visualmente al
  recorrer la lista. Verificado con un usuario inactivo de prueba
  insertado en BD y luego borrado.
- **#24** `ThumbnailPanel`: hint persistente arriba del listado de
  páginas — *"Botón derecho en una página para más opciones (excluir,
  marcar revisión, eliminar...)"* — más `cursor-context-menu` y `title`
  HTML en cada wrapper. El usuario QA no descubría las acciones del
  menú contextual sin pista.

Verificado visualmente con Playwright. Tests pasaron de 391 → 397.

### 4.2 Drag-drop de ficheros al workbench (#8) — commit `a734e13`

Zona de drop en todo el `WorkbenchView`. Cuando se arrastran ficheros
desde el explorador, aparece un overlay con borde discontinuo:

> ↥ Suelta los ficheros para subirlos
> PDF, TIFF y imágenes (PNG, JPG)

Reusa el mismo `onUpload(files)` del botón "↑ Subir". Detalles:

- `dragCounter` para evitar parpadeo cuando el cursor entra/sale de
  hijos del root.
- Filtro `dragHasFiles`: solo activa overlay si `dataTransfer.types`
  incluye `"Files"` — drag de texto/HTML no levanta drop zone.
- `canUpload` computed que combina los refs locales (`uploading`,
  `running`, `transferring`, `exporting`) con `isReadOnly` (state del
  batch).

**Bug encontrado por el test TDD antes del commit**: la primera versión
de `canUpload` solo consultaba los refs locales. Si el batch estaba en
state=`running` global pero el ref local no, el overlay aparecía igual
— inconsistente con el botón "↑ Subir" que sí está deshabilitado en
ese caso. Test del caso `state=running` falló → añadido
`!isReadOnly.value` al computed.

4 tests TDD nuevos. Verificado e2e con Playwright dropeando 2 PNGs
sobre el workbench → 4 páginas tras refresh. BD restaurada después.

### 4.3 Filtros + paginación en `/batches` (#3) — commit `8b554ce`

Último gap funcional de la bitácora. El backend ya soportaba
`application_id`, `state`, `limit` y `offset` como query params;
faltaba la UI.

Cambios en `stores/batches.ts`:

- `fetchAll` acepta un objeto con `applicationId`, `state`, `limit`,
  `offset` y `silent`. (Compat temporal con la firma vieja `(number)`,
  eliminada en el simplify posterior).
- Expone `limit` y `offset` reactivos además de `items` y `total`.

Cambios en `BatchListView`:

- Barra superior con dos selects (aplicación, estado) que disparan
  refetch automático y resetean offset a 0 al cambiar.
- Indicador "X–Y de N" en la esquina derecha; "Sin resultados" cuando
  `total=0`.
- Empty state diferenciado: *"No hay lotes que coincidan con los
  filtros"* cuando hay filtros activos vs el genérico cuando no.
- Controles "‹ Anterior / Siguiente ›" con `disabled` en los bordes;
  ocultos cuando `total ≤ PAGE_SIZE` (50).
- Polling silencioso ahora respeta los filtros activos.

7 tests TDD nuevos. Verificación visual con Playwright cubrió:
filtros visibles con "1–1 de 1", filtro `state=running` produce empty
state filtrado, reset del filtro hace reaparecer el lote.

## 5. Refactor simplify — commit `0e0efe6`

Tres agentes en paralelo (reuse / quality / efficiency) revisaron el
diff de los tres commits funcionales del día. Hallazgos accionables:

1. **`STATE_OPTIONS` en `BatchListView` duplicaba el switch de
   `BatchStateBadge`** con labels inconsistentes (`"Procesando"` vs
   `"procesando…"`). Extraído a `components/batches/batchStates.ts`
   con tipo `BatchState`, array `BATCH_STATES` y helper
   `getBatchStateInfo()`. Badge y filtro consumen la misma fuente;
   capitalización uniforme.
2. **Firma `fetchAll(number | object)` con type guard interno** era el
   peor de dos mundos: API compleja para preservar 2 callers antiguos.
   Migrados (`ApplicationDetailView`, `DashboardView`) a la firma de
   objeto y eliminada la rama legacy. `fetchAll` ahora acepta SOLO un
   objeto opcional.
3. **`apps.fetchAll()` y `load()` secuenciales en `onMounted`**
   independientes. Paralelizados con `Promise.all` → 1 RTT menos en
   cold load.
4. **Guard redundante `if (!isDragging.value) return` en
   `onDragLeave`**. Reemplazado por `if (dragCounter === 0) return`
   — más coherente con el patrón counter del resto del componente.

Falsos positivos descartados: `currentOffset` vs `store.offset` —
coexistencia razonable (intent local pre-fetch vs respuesta servidor);
`usePagination` genérico — no urgente, ningún otro caller pagina hoy.

## 6. Roadmap acordado para próximas sesiones

Tras cerrar la bitácora QA, el usuario priorizó las siguientes piezas
para llegar a una primera versión SaaS publicable. Cada una merece
sesión dedicada con plan previo.

1. **Superadmin + creación de tenants/usuarios** (plataforma).
2. **Cliente local web** (escáner TWAIN/SANE + transferencia local
   desde el navegador, vía agente local).
3. **Editor de scripts web** (Monaco con preview).
4. **Multiidioma (i18n)** — frontend con `vue-i18n`, backend con
   catálogos.
5. **Smoke test extenso e2e** cubriendo todas las posibilidades.
6. **Primera versión SaaS publicable (v0.2.0 web)** con CI que
   construya/publique imagen Docker (GHCR).
7. **AI Mode web** — al final, confirmado por el usuario.

Documentado en memoria como `project_roadmap_post_qa.md`.

## Estado al cerrar la primera tanda (mañana)

- Rama `feature/web` HEAD `0e0efe6`, sincronizada con `origin`.
- Rama `main` con merge consolidado, sincronizada con `origin`.
- Tests web frontend: **408 / 408** pasando (vs 391 al inicio del día).
- Bitácora QA web 2026-04-27: **40/40 entradas resueltas** ✅

## Commits de la mañana

```
0e0efe6  refactor(web-frontend): simplifica tras revisión simplify
8b554ce  feat(web-frontend): filtros + paginación en /batches (QA #3)
a734e13  feat(web-frontend): drag-drop de ficheros al workbench (QA #8)
0e4f248  fix(web-frontend): refinamientos UX QA #1 #4 #24
61fdb47  merge: feature/web → main — versión web SaaS DocScan Studio
d5726be  chore: actualiza path del filesystem MCP server a DocScan
```

---

# Sesión de tarde — Sprint Superadmin (hitos 1-6 backend)

Tras cerrar la bitácora QA, se atacó el primer item del roadmap
post-QA: **Superadmin + creación de tenants y usuarios**, con la
jerarquía `superadmin (TecnoMedia, cross-tenant) > company_admin
(por tenant) > operator`.

El sprint se dividió en 11 hitos. Esta sesión cerró los **6 primeros**
(todo el backend); los hitos 7-11 (frontend + smoke) quedan para la
siguiente.

## Diseño consensuado al inicio

Decisiones clave acordadas con el usuario antes de implementar:

1. **Superadmin pertenece a un tenant especial "TecnoMedia"**
   (slug `tecnomedia`, plan `enterprise`). Sus poderes vienen del
   rol, no de pertenencia. Evita hacer `tenant_id` nullable.
2. **Bootstrap por CLI idempotente**: `python -m web.api.bootstrap
   superadmin --email X --password Y --display Z`. No Alembic ni
   env-only.
3. **Registro público desaparece**. Solo el superadmin crea tenants.
4. **Sin impersonación en este sprint** (superpoder peligroso, se
   pospone).
5. **Auditoría mínima desde ya**: tabla `audit_logs` simple sin UI,
   solo BD para forensics. Todas las acciones admin se loggean.
6. **Hard-delete de tenant cascade real**: BD (applications →
   batches → pages, invitations, users) + archivos del storage.
7. **Plan = enum cerrado** `free | basic | enterprise`. Sin lógica
   de cuotas todavía.
8. **Multi-superadmin permitido** con guard "al menos uno activo".

## Hitos cerrados

### Hito 1 — CLI bootstrap (`web/api/bootstrap.py`)

`bootstrap_superadmin(db, *, email, password, display_name)` crea o
reutiliza tenant TecnoMedia y un primer superadmin. Idempotente. Si
el email pertenece a otro usuario que no es superadmin de TecnoMedia,
levanta `BootstrapError`. CLI con argparse, validación email regex
y password ≥ 8 caracteres. **11 tests TDD**.

### Hito 2 — Eliminar registro público

- Endpoint `POST /api/auth/register` borrado (404 ahora).
- `RegisterView.vue` y ruta `/register` eliminadas. Función
  `register()` del store, tipo `RegisterRequest`, enlace
  "Registrarse" del LoginView — todo eliminado.
- Helper de tests `_create_tenant_and_admin(factory, ...)` sustituye
  los 7 usos directos del antiguo POST. La factory se expone vía
  `app.state.test_db_factory` para no cambiar la firma de las 188
  invocaciones de `_auth_header`.
- `TestRegister` (4 tests) reemplazada por `TestRegisterRemoved`
  (1 test que verifica 404).

### Hito 3 — Bloquear login con tenant suspendido

`_authenticate` y `get_current_user` rechazan con 401 "Tenant
suspendido" si el tenant del usuario tiene `active=False`. Cubre
login JSON, `/token` form-data y tokens JWT ya emitidos. **3 tests TDD**.

### Hito 4 — Modelo AuditLog + helper `audit()`

Tabla `audit_logs` (id, actor_user_id [FK users, nullable], tenant_id
[FK tenants, nullable], action, target_type, target_id, payload_json,
created_at) con índices en actor, tenant, action, target_type,
created_at.

Helper `audit(db, *, actor, action, target_type, target_id, payload=None)`:
- `actor=None` permite acciones de sistema (cron, recovery).
- Solo `add+flush`; el commit lo controla el caller.

Migración Alembic `a3b8d4e2f7c9_add_audit_logs.py` (down_revision
`f7c9d2b85a43`). Verificada por
`test_alembic_upgrade_head_matches_create_all`. **9 tests TDD + 2 migration**.

### Hito 5 — Router `/api/admin/tenants`

5 endpoints, todos con `require_role(ROLE_SUPERADMIN)`, todos los
mutating registran audit_log:

- `GET /api/admin/tenants` — lista paginada con stats `(n_users,
  n_apps, n_batches)`.
- `POST /api/admin/tenants` — crea tenant + primer company_admin
  atómico. Valida plan ∈ ALLOWED_PLANS, slug único, email único,
  slug "tecnomedia" reservado.
- `GET /api/admin/tenants/{id}` — detalle con `users[]`.
- `PATCH /api/admin/tenants/{id}` — actualiza name | plan | active.
  No permite suspender TecnoMedia (`409`). Audit solo si hay cambios.
- `DELETE /api/admin/tenants/{id}` — hard-cascade: rechaza si
  batches en `running`/`transferring` (`409`), protege TecnoMedia
  (`409`), borra applications (cascade BD a batches → pages),
  invitations, users (relación con cascade), tenant. Limpia
  archivos del storage de cada page antes del delete BD.

**32 tests TDD**.

### Hito 6 — Router `/api/admin/users`

4 endpoints cross-tenant con guards:

- `GET /api/admin/users` — lista global con filtro `?tenant_id=`
  opcional. Cada item incluye `tenant_name` resuelto.
- `POST /api/admin/users` — crea usuario directo en cualquier
  tenant con cualquier rol (incluyendo superadmin). Valida rol,
  password, email único.
- `PATCH /api/admin/users/{id}` — cross-tenant cambia role | active
  | display_name. **Bloquea self-modify** (defense-in-depth) y
  aplica guards "último superadmin global activo" y "último
  company_admin activo del tenant".
- `DELETE /api/admin/users/{id}` — mismos guards que PATCH.

**26 tests TDD**.

## Métricas del sprint backend

- **Tests añadidos**: **+81** (11 + 1 + 3 + 9 + 2 + 32 + 26 - 4 borrados).
- **Suite web total**: **298 passing** (vs 217 al cerrar la bitácora QA = +81).
- **Tests frontend**: 408/408 (sin cambios — todo backend en esta tanda).
- **Sin regresiones**.

## Commits de la tarde

```
c18af8c  feat(web): routers admin de tenants y usuarios cross-tenant (hitos 5-6)
edbc525  feat(web): añadir AuditLog model + helper audit() (hito 4 sprint)
efb94ef  feat(web): preparar plataforma para superadmin (hitos 1-3 sprint)
```

## Pendiente para próxima sesión

| Hito | Descripción |
|---|---|
| 7 | Frontend store admin + tipos + API client |
| 8 | Frontend rutas + guards + layout condicional (sidebar superadmin) |
| 9 | Vista TenantListView + CreateTenantView |
| 10 | Vista TenantDetailView (editar tenant + sus usuarios) |
| 11 | Smoke e2e contra docker + doc deploy + README "Bootstrap inicial" |

El backend está listo para consumirse desde Vue. Las decisiones de
diseño (jerarquía de roles, endpoints, schemas, audit) ya están
fijadas — el frontend será trabajo de UI sin sorpresas
arquitecturales.

## Estado al cerrar la sesión de tarde

- Rama `feature/web` HEAD `c18af8c`, **3 commits** por delante de
  `origin/feature/web` (push al cierre).
- Backend del sprint superadmin completo y testeado.
- Tareas pendientes del sprint en memoria.

---

# Sesión de noche — Sprint Superadmin frontend (hitos 7-11)

Continuación inmediata: con el backend cerrado en la tarde, esta sesión
ataca los **5 hitos restantes** del sprint (todo frontend + smoke).

Los 5 hitos se cierran en una sola tanda y **el sprint queda completo**.

## Hitos cerrados

### Hito 7 — Store Pinia `admin.ts` + tipos TypeScript (commit `9958d78`)

- `web/frontend/src/api/types.ts` recibe los 9 tipos que reflejan
  los schemas Pydantic del backend: `TenantStats`, `TenantListItem`,
  `TenantUserItem`, `TenantDetail`, `TenantCreateRequest`,
  `TenantUpdateRequest`, `AdminUserListItem`, `AdminUserCreateRequest`,
  `AdminUserUpdateRequest`.
- `web/frontend/src/stores/admin.ts` expone:
  - `fetchTenants({limit, offset})`, `fetchTenant(id)`, `createTenant`,
    `updateTenant`, `deleteTenant`.
  - `fetchUsers({limit, offset, tenantId})`, `createUser`, `updateUser`,
    `deleteUser`.
  - `updateUser` y `deleteUser` refrescan automáticamente
    `currentTenant` cuando el usuario afectado pertenece al tenant
    cargado.
- **15 tests TDD** mockeando `api.get/post/patch/delete` cubren la
  lista, paginación, refresh cross-cabal y propagación de errores.

### Hito 8 — Rutas `/admin/*` + guard de rol + sidebar dual (commit `123129a`)

- **`web/frontend/src/router/roleGuard.ts`** — función pura testeable
  `enforceRoleAccess(to, user)`:
  - Rutas `meta.guest` siempre pasan (login, accept-invitation).
  - User no cargado pasa (otros guards de auth deciden).
  - `meta.superadmin` + rol distinto → redirect `/`.
  - rol `superadmin` + ruta no `/admin/*` → redirect `/admin/tenants`.
- **`router/index.ts`** añade las 3 rutas (`/admin/tenants`,
  `/admin/tenants/new`, `/admin/tenants/:id`) con `meta.superadmin: true`.
  El `beforeEach` hidrata `auth.user` (si hay token) antes de aplicar
  el guard.
- **`AppLayout.vue`** condiciona el sidebar y el header según el rol:
  - `superadmin` → header *"TecnoMedia · Administración"* y sidebar
    con un único enlace **Tenants**.
  - resto → sidebar normal (Inicio · Aplicaciones · Lotes · [Equipo]).
  - Atributo `data-superadmin` en el `<aside>` para QA.
- **11 tests** del guard puro + **3 tests** del AppLayout cubriendo
  los 3 roles. 3 vistas admin como stubs temporales.

### Hito 9 — `TenantListView` + `CreateTenantView` (commit `7f9e4d8`)

- **`TenantListView.vue`**: tabla con stats (usuarios/apps/lotes),
  badge coloreado por plan, toggle Activo/Suspendido inline, eliminar
  con confirmación por `prompt` del nombre exacto del tenant. Tenants
  inactivos atenuados con `opacity-50`. CTA "Nuevo tenant" en cabecera
  + empty-state con CTA cuando no hay ninguno. **7 tests**.
- **`CreateTenantView.vue`**: formulario en dos fieldsets (Empresa /
  Primer administrador). Validación local de password ≥8 caracteres
  antes del POST. Errores 409 (email duplicado, plan inválido) en panel
  inline. Tras éxito redirige a `/admin/tenants/:id`. **4 tests**.

### Hito 10 — `TenantDetailView` con CRUD de usuarios (commit `eebd933`)

Vista más cargada del frontend admin. Cubre tres bloques:

1. **Cabecera + stats** — badge Activo/Suspendido + toggle inline,
   grid de stats (usuarios, aplicaciones, lotes).
2. **Datos del tenant** — name + plan + fecha creado. Save *dirty-aware*
   (envía sólo los campos modificados). Feedback de error inline.
3. **Tabla de usuarios** — role select (3 opciones), toggle Activo,
   delete con `confirm`. Los controles del propio usuario
   deshabilitados (defense-in-depth — el backend ya devuelve
   `409 _SELF_MSG`). Errores de los endpoints `/admin/users` afectando
   a la lista (p.ej. último company_admin) se renderizan como banner
   rojo encima de la tabla.
4. **Modal "Crear usuario"** — patrón `role=dialog` + `aria-modal="true"`
   reutilizado del workbench. Validación local password ≥8. Errores
   API se muestran inline sin cerrar el modal.

**12 tests TDD** cubren: carga, edición de tenant (dirty/save + toggle
active), CRUD de usuarios (rol/active/delete con confirm/cancel),
guard 409 último-admin desde el backend, modal crear (éxito + error
API + password corta).

### Hito 11 — Smoke e2e + docs README + CHANGELOG (commit `20bbc79`)

- **`scripts/smoke_superadmin.sh`** — matriz curl de **21 casos**
  contra el stack docker compose:
  registro 404 · login + me · /admin/* con 3 roles (sin token, super,
  company_admin) · CRUD tenants · plan inválido 422 · nombre duplicado
  409 · CRUD users cross-tenant · self-modify 409 · suspender tenant
  → login bloqueado 401 · cascade delete · login con email de tenant
  borrado 401 · proteger TecnoMedia 409.
  Validado contra `docker compose up` con bootstrap previo: **21/21 PASS**.
- **`docs/screenshots/smoke-superadmin/`** — 4 capturas Playwright del
  flujo visual:
  1. Listado de tenants (sidebar reducido, header TecnoMedia,
     2 tenants con stats).
  2. Formulario `/admin/tenants/new`.
  3. Detalle del tenant recién creado (PlaywrightCo) con CRUD de
     usuarios.
  4. Bloqueo del guard al loguear como `company_admin` e intentar
     navegar a `/admin/tenants` → redirect a `/`.
- **README.md** sección nueva *"🌐 Versión web SaaS"* con docker
  compose + bootstrap del superadmin + jerarquía de roles documentada
  + referencia al script smoke.
- **CHANGELOG.md** entrada `[Unreleased]` documentando el sprint
  completo (hitos 1-11), endpoints añadidos, migración Alembic
  `a3b8d4e2f7c9`, +130 tests TDD del sprint.

## Hallazgos durante el smoke contra docker

- **TLDs reservados rechazados por `EmailStr`**: el primer bootstrap
  con `--email super@tecnomedia.test` creó al usuario (la regex de
  `web/api/bootstrap.py` es laxa) pero el endpoint `/auth/login`
  validó con `EmailStr` y rechazó `.test`/`.example`/`.invalid`/
  `.localhost` con 422. Cambiado el bootstrap a `super@tecnomedia.es`.
  Documentado en el script de smoke (que también usa `.es` para los
  emails del tenant SmokeAcme).
- **Imagen del API obsoleta**: el container montado por compose era
  anterior al commit `efb94ef` (sin `web/api/bootstrap.py`).
  `docker compose build api worker` necesario tras los cambios de
  estructura del paquete `web/api`. Ya documentado.

## Validación visual con Playwright

Levantado vite (`localhost:5173` con proxy `/api → :8001`), login del
superadmin, navegación a `/admin/tenants/new`, creación de
`PlaywrightCo` (plan basic, admin@playwrightco.es), redirect
automático al detalle, logout, login como `admin@playwrightco.es`
(company_admin), intento manual a `/admin/tenants` → redirect a `/`.
**Comportamiento esperado en todos los pasos.**

## Métricas del sprint completo (1-11)

- **Tests añadidos**: **+130** (81 backend + 49 frontend).
- **Suite web backend**: 298 passing.
- **Suite web frontend**: **460 passing** (vs 408 al inicio del sprint
  = +52 frontend específicos del sprint, pero la cuenta exacta es
  +49 nuevos tests admin tras la refactorización de los antiguos).
- **vue-tsc**: limpio en cada commit.
- **Sin regresiones**.

## Commits de la noche

```
20bbc79  docs: cierra sprint superadmin con smoke + README + CHANGELOG (hito 11)
eebd933  feat(web/frontend): TenantDetailView con CRUD de usuarios (hito 10)
7f9e4d8  feat(web/frontend): TenantListView + CreateTenantView (hito 9 sprint)
123129a  feat(web/frontend): rutas /admin + guard de rol + sidebar superadmin (hito 8)
9958d78  feat(web/frontend): store admin + tipos para superadmin (hito 7 sprint)
```

## Estado al cerrar el día

- **Sprint Superadmin: 11/11 hitos cerrados ✅**.
- Rama `feature/web` HEAD `20bbc79`, **5 commits** por delante de
  `origin/feature/web` (push al cierre).
- Plataforma SaaS multi-tenant operativa: superadmin TecnoMedia
  gestiona tenants y usuarios cross-tenant; company_admins gestionan
  invitaciones y operadores en su propio tenant; operadores trabajan
  con sus lotes.
- Próxima prioridad del roadmap: **cliente local web** (escáner
  TWAIN/SANE + transferencia local desde el navegador, vía agente
  local).
