# Progreso 2026-07-22 — Suite no interactiva, Python 3.14, webhook, upgrades y fix de arranque (v0.1.4 y v0.1.5)

## Resumen

Sesión de trabajo sobre `main`, continuación del saneamiento del desktop cerrado
ayer (release v0.1.3). Se atacaron los follow-ups pendientes de la auditoría: se
hizo la batería de tests totalmente automática (sin clics ni contraseñas de
administrador), se eliminó de raíz la divergencia de versiones de Python que
había causado un bug de portabilidad, se puso al día el informe de proyecto, se
limpió el repositorio y se implementó una funcionalidad nueva de extremo a
extremo: **notificaciones webhook configurables por aplicación**.

En una segunda tanda del mismo día se acometió, además, el último follow-up de
mayor riesgo de la auditoría: la **actualización de las dependencias mayores**
(visión por computador, SDK de IA e interfaz gráfica), pendiente de una sesión
dedicada por su potencial de incompatibilidades.

## 1. Batería de tests no interactiva y sin hardware

La suite podía quedarse bloqueada esperando que alguien pulsara «Aceptar» en un
diálogo, y algún test intentaba abrir el escáner físico por USB, lo que hacía
que el sistema pidiera **contraseña de administrador** y tardaba más de 30
segundos. Ambos problemas se han cerrado con una configuración global de tests
(`tests/conftest.py`) que: simula todos los diálogos de la interfaz para que
nunca esperen una respuesta manual; sustituye la capa del escáner por un doble
inofensivo para que ningún test toque hardware real; y descarta automáticamente
los tests de la parte web archivada cuando no están sus dependencias.

Como consecuencia, la batería de integración continua se simplificó: ya no hace
falta enumerar a mano los tests a excluir, porque la nueva configuración lo
resuelve sola.

### Tests

`pytest` ahora se ejecuta sin ninguna intervención: **861 tests en ~24 segundos**,
cero interacción, cero acceso a hardware.

Commits: **`41dcc6d test: conftest global que hermetiza la suite`**,
**`e4f84ef ci: delega el descarte de tests web al conftest`**

## 2. Unificación del proyecto en Python 3.14

El proyecto se desarrollaba en Python 3.14 pero la integración continua y los
instaladores se construían con Python 3.13. Esa divergencia fue la que provocó,
en la sesión anterior, un bug de portabilidad que impedía importar el código en
3.13. Mantener dos versiones era una fuente de errores latentes, y —al tratarse
de una aplicación empaquetada que lleva su propio intérprete— la compatibilidad
con 3.13 no aportaba nada al usuario final.

Se decidió **unificar toda la cadena en Python 3.14**: desarrollo, integración
continua, documentación e instaladores usan ahora la misma versión. Se verificó
previamente que todas las librerías críticas (interfaz gráfica, visión por
computador, imágenes, PDF y el empaquetador) funcionan en 3.14. La integración
continua se ejecutó tras el cambio y quedó **en verde**, confirmando que la
divergencia está cerrada de raíz.

Commits: **`4e6f5d9 build: unifica el toolchain en Python 3.14`**,
**`b58feab style: ruff format bajo py314`**

## 3. Puesta al día del informe de proyecto

El documento base `docs/INFORME_PROYECTO.md` estaba fechado en marzo y reflejaba
un 91 % de avance. Se actualizó a la versión 1.1 con las cifras reales: proyecto
completado (22 de 22 fases), 104 módulos de código, 861 tests, cuatro releases
públicas con instaladores y actualización automática, y runtime en Python 3.14.
La parte descriptiva (objetivos, arquitectura, interfaz) se conservó intacta.

Commit: **`6f56cbf docs: actualiza INFORME_PROYECTO al estado actual (v1.1)`**

## 4. Notificaciones webhook configurables por aplicación

Hasta hoy, el sistema tenía el motor de notificaciones implementado pero **sin
forma de configurarlo**: al terminar la transferencia de un lote, el aviso HTTP
(webhook) nunca se enviaba porque no había dónde guardar su dirección. Se ha
cerrado ese hueco de principio a fin.

Ahora cada aplicación tiene una pestaña nueva, **«Notificaciones»**, en su
configurador, donde se define la dirección del webhook, el método, las cabeceras
HTTP y el tiempo de espera, con un interruptor para activarlo o desactivarlo. Al
completar (o fallar) la transferencia de un lote, el proceso desatendido envía
automáticamente el aviso a esa dirección con los datos del lote. La configuración
se guarda por aplicación y viaja al exportar o importar aplicaciones.

La estructura de datos se diseñó dejando sitio para el envío por email (SMTP) en
el futuro sin necesidad de una nueva migración de base de datos.

### Archivos nuevos

| Archivo | Aporta |
|---|---|
| `app/ui/configurator/tabs/tab_notifications.py` | Pestaña «Notificaciones» del configurador |
| `alembic/versions/c1d5e9f34b28_...py` | Migración de la columna nueva `notifications_json` |
| `tests/test_notification_config.py` | Tests del cargador de configuración |
| `tests/test_tab_notifications.py` | Tests de la pestaña de interfaz |

### Tests

18 tests nuevos cubriendo el cargador de configuración, el cableado en el proceso
desatendido (webhook activado, desactivado y en caso de error) y la pestaña de
interfaz. Migración verificada con subida y bajada sobre una base de datos de
prueba.

Commit: **`6c5aff7 feat: webhook de notificaciones configurable por aplicación`**

## 5. Actualización de dependencias mayores

El proyecto arrastraba varias librerías clave rezagadas respecto a sus versiones
actuales, y tres de ellas implicaban saltos de riesgo: la librería de **visión
por computador** (OpenCV), que estrenaba versión mayor 5.0 con cambios de fondo;
el **SDK de inteligencia artificial** de Anthropic, con más de treinta versiones
de diferencia; y el **framework de interfaz gráfica** (PySide6). Actualizarlas se
había reservado para una sesión propia por el riesgo de romper la aplicación.

El trabajo se hizo por fases y con red de seguridad: se partió de una rama
dedicada y de una copia de respaldo del entorno para poder revertir, y **tras
actualizar cada grupo de librerías se ejecutó la batería completa de tests**
(879 en cada pasada), agrupando cada grupo en su propio commit para poder aislar
cualquier problema. Antes de tocar nada se comprobó, con la documentación oficial
de cada librería, que los cambios de fondo no afectaban a cómo el proyecto las
usa —en el caso de OpenCV, la aplicación solo emplea funciones básicas de
tratamiento de imagen, ajenas a las partes que la versión 5 reorganiza— y que
existían paquetes compatibles con Python 3.14 para todas.

El resultado es que las siete librerías quedaron al día **sin necesidad de
cambiar una sola línea de código**: ninguna de las funciones que el proyecto
utiliza cambió de forma. Además de los tests, se ejecutó una comprobación de
arranque que carga los servicios de imagen, código de barras e IA y construye los
clientes de IA reales, todo correcto.

### Librerías actualizadas

| Librería | Antes | Ahora |
|---|---|---|
| opencv-python (visión) | 4.13 | **5.0.0.93** (versión mayor) |
| anthropic (SDK IA) | 0.84.0 | **0.117.1** |
| PySide6 (interfaz) | 6.10.2 | **6.11.1** |
| SQLAlchemy (BD) | 2.0.48 | 2.0.51 |
| zxing-cpp (código de barras) | 3.0.0 | 3.1.0 |
| PyMuPDF (PDF) | 1.27.1 | 1.28.0 |
| openai (SDK IA) | 2.26.0 | 2.46.0 |

### Salvedad de validación

La actualización se validó con la batería de tests y una comprobación de
arranque de los servicios, **pero no con un arranque real de la interfaz gráfica
ni del escáner** (en el entorno de tests el escáner está simulado y no hay
hardware). Antes de publicar una release con estas versiones conviene un arranque
manual de la ventana de trabajo para descartar cualquier detalle visual de la
nueva versión de la interfaz gráfica.

Commits: **`2d9a35c build: upgrade deps de bajo riesgo`**,
**`71a3573 build: upgrade PySide6 6.10.2 -> 6.11.1`**,
**`7b13657 build: upgrade anthropic 0.84.0 -> 0.117.1`**,
**`babdb56 build: upgrade opencv-python 4.13 -> 5.0.0.93 (major)`**,
**`fcd1202 docs: sincroniza README y CHANGELOG con los upgrades`**.
Con el stack actualizado se publicó la **versión 0.1.4** (que recoge también el
webhook de notificaciones).

## 6. Validación en campo del nuevo stack

Para cerrar la salvedad del apartado anterior se arrancó la aplicación **real**
(no solo la batería de tests) con el stack actualizado: la ventana de inicio, el
configurador con sus siete pestañas y la ventana de trabajo, además de una
ejecución completa del procesamiento de imagen con la nueva versión de OpenCV.
Todo se comprobó visualmente y funcionó correctamente: la interfaz gráfica nueva
pinta bien y el procesamiento de imagen no se ve afectado.

Esta validación resultó **decisiva**: al arrancar la aplicación de verdad contra
la base de datos real apareció un fallo que la batería de tests no podía ver
(porque los tests siempre parten de una base de datos nueva). Ese hallazgo abrió
el trabajo del apartado siguiente.

## 7. Fallo de arranque contra bases de datos existentes y su corrección (v0.1.5)

**El problema.** La aplicación no arrancaba contra una base de datos creada por
una versión anterior: fallaba porque le faltaba una columna nueva. La causa de
fondo es que, al arrancar, la aplicación creaba las tablas que faltaran pero
**nunca añadía columnas nuevas** a las tablas ya existentes, ni aplicaba las
migraciones de base de datos. Cualquier usuario con datos previos que actualizara
se quedaba sin poder abrir la aplicación. El fallo afectaba a la versión 0.1.4
recién publicada.

**Reparación inmediata.** Se reparó la base de datos de trabajo de forma
verificada: copia de seguridad, prueba del arreglo sobre una copia, y solo
después aplicación sobre la real, conservando las once aplicaciones configuradas.

**Corrección de fondo.** Se cambió el arranque para que la aplicación **aplique
las migraciones de base de datos automáticamente**, de forma robusta ante los tres
escenarios posibles: base de datos nueva, base de datos ya versionada, y base de
datos «heredada» de una versión anterior (a la que se le completan las columnas
que falten y se pone bajo control de versiones). Los datos existentes se
preservan siempre.

**Validación reforzada.** Antes de publicar se generó el instalador localmente y
se arrancó el **programa ya empaquetado** contra una base de datos antigua. Esta
precaución cazó un segundo problema —una pieza del sistema que no se incluía en el
paquete— que habría vuelto a romper la versión publicada igual que le pasó a la
0.1.4. Corregido y vuelto a validar, se publicó la **versión 0.1.5**, que corrige
el fallo de arranque. Quien tenga la 0.1.4 y sufriera el problema, lo verá
resuelto sin hacer nada al actualizar.

### Tests

Cuatro tests nuevos que cubren los tres escenarios de base de datos y el arreglo
de la migración conflictiva. La batería pasa de 879 a **883**.

Commits: **`88fdf33 fix: la app desktop aplica migraciones alembic al arrancar`**,
**`b98403e release: v0.1.5`**

## Estado actual del proyecto

- **Rama**: `main`, sincronizada tras el push de cierre.
- **Tests totales**: **883 passing**, 0 fallos.
- **Versiones publicadas hoy**: **v0.1.4** (webhook + upgrades de dependencias) y
  **v0.1.5** (corrección del fallo de arranque). Ambas con instaladores para
  Linux y Windows generados por integración continua.
- **Runtime**: Python 3.14 en desarrollo, CI e instaladores.
- **Dependencias**: siete librerías actualizadas, incluidas OpenCV 5.0 (mayor),
  Anthropic 0.117 y PySide6 6.11; documentación (README/CHANGELOG) sincronizada.
- **Lint**: `ruff check .` limpio.

## Decisiones tomadas

- **Unificar en Python 3.14 en vez de mantener 3.13**: al ser una aplicación
  empaquetada con su propio intérprete, la compatibilidad con 3.13 no beneficia
  al usuario final; unificar elimina la divergencia que causaba bugs.
- **Solo webhook ahora, email SMTP después**: el email introduce el manejo de
  credenciales SMTP, que exigen cifrado; se acota el alcance para entregar valor
  antes. La estructura de datos ya contempla el email.
- **Columna y pestaña dedicadas para notificaciones**: separación limpia frente a
  mezclar la configuración con la de transferencia o la de IA.
- **Borrar la rama `production`**: no tenía ningún cambio propio, todo estaba ya
  en `main`.
- **Migraciones como fuente de verdad también en la aplicación de escritorio**:
  frente a un remiendo que solo añadiera columnas, se optó por que la aplicación
  aplique las migraciones al arrancar, igual que ya hace la parte web. Es el
  mecanismo correcto a largo plazo y cubre cualquier cambio futuro de la base de
  datos (no solo añadir columnas).
- **Probar el instalador antes de publicar**: generar el programa empaquetado y
  arrancarlo localmente antes de etiquetar una versión. Esta práctica evitó
  repetir el tipo de fallo de la 0.1.4 (algo que funciona en desarrollo pero se
  rompe en el instalador).

## Próximos pasos (en orden)

1. **Email SMTP de notificaciones**: añadir la interfaz y el cableado, con
   **cifrado Fernet** del password SMTP (regla de seguridad del proyecto).
2. **Prueba con escáner físico real**: arranque manual con el escáner conectado
   para completar la validación de captura de principio a fin (lo único del stack
   nuevo que no puede probarse sin hardware).

## Archivos clave

- `app/db/database.py` — aplicación de migraciones al arrancar (`run_migrations`).
- `tests/conftest.py` — configuración global que hace la batería no interactiva y sin hardware.
- `app/services/notification_service.py` — motor de notificaciones + cargador de configuración.
- `app/ui/configurator/tabs/tab_notifications.py` — pestaña de configuración del webhook.
- `docscan_worker/worker_main.py` — envío del webhook al completar/fallar la transferencia.
- `ruff.toml`, `.github/workflows/ci.yml`, `.github/workflows/release.yml` — toolchain unificado en 3.14.
- `requirements.txt` — versiones de las siete dependencias actualizadas.
