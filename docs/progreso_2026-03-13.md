# Progreso 2026-03-13

## Resumen de la sesión

Sesión de pruebas manuales completa (tests 4-15 del MANUAL_TEST_GUIDE.md). Todos los tests pasados y marcados como completados.

## Cambios realizados

### Scanner (tests 10-11)
- **SANE**: `acquire()` reimplementado usando `scanimage` como subprocess — soluciona problemas de threading con python-sane (SANE no es thread-safe entre hilos)
- **Diálogo dinámico de configuración**: `ScannerConfigDialog` lee las opciones reales del dispositivo SANE (`dev.opt`) y genera controles apropiados (combos, spinboxes, checkboxes)
- **TWAIN/WIA**: soporte para diálogo nativo del driver (`show_ui` flag)
- Checkbox "Configurar" en toolbar del workbench
- Feedback al buscar escáneres: cursor de espera + mensaje en barra de estado
- `ScanConfig` ampliado con `show_ui` y `extra_options`
- Instancia de scanner reutilizable en workbench (evita múltiples `sane.init()`)
- `scanner_backend` default cambiado de "twain" a "" (autodetección por plataforma)
- Probado con Canon imageFORMULA DR-M160 via SANE

### Log panel
- Nuevo `LogPanel` como pestaña en `MetadataPanel` (junto a "Lote")
- Handler de logging que emite señales Qt para thread-safety
- Color por nivel: gris (DEBUG), blanco (INFO), amarillo (WARNING), rojo (ERROR)

### Script stubs (test 4)
- Reescritos como declaraciones Python reales (no comentarios) para compatibilidad Pylance/Pyright
- Clases con docstrings: BarcodeResult, PageFlags, AppContext, BatchContext, PageContext, PipelineContext

### Campos de lote (test 5)
- Nuevo tipo "fecha" con QDateEdit y calendario popup
- Estilos QSS para QDateEdit consistentes con QLineEdit
- Persistencia de campos de lote al cambiar valores

### Transferencia (tests 8-9)
- Interpolación de `{first_barcode}` en patrones de nombre
- Normalización de claves de campos (espacios → guiones bajos)
- Botón "Examinar…" para seleccionar carpeta destino
- Tooltips explicativos en todos los campos de configuración
- Auto-reset de lote tras exportación exitosa

### Navegación (test 12)
- Quitados botones "siguiente barcode" y "siguiente revisión" del overlay
- Nuevo botón "Navegación programable" (engranaje) con evento `on_navigate_script`
- `_fire_event` pasa `current_page_index` y `total_pages` a eventos de navegación
- `_on_nav_script` construye resumen de páginas (barcodes, needs_review) para el script
- `ScriptEngine.run_event` filtra kwargs según firma de la función (retrocompatible)

### Eventos
- Marcador `[*]` en combo de eventos para los que tienen código definido
- Nuevo evento `on_navigate_script` en la lista de eventos

### UI
- Page counter: 36px, bold, color rojo (#f38ba8 dark / #d20f39 light)
- Cerrar lote ahora cierra la ventana del workbench
- Navegación por thumbnails optimizada (get_by_id en vez de get_by_batch)

## Archivos nuevos
- `app/ui/workbench/log_panel.py`
- `app/ui/workbench/scanner_config_dialog.py`

## Estado actual
- **Todos los 15 tests manuales completados**
- Rama `release_1` creada para revisión y pulido
- Tests pytest: 494 passing (errores preexistentes por modelo OperationHistory no definido)

## Próximos pasos
- Revisión exhaustiva del código en rama `release_1`
- Pulir detalles pendientes
- Merge a main
