# Progreso 2026-04-09 — API web: CRUD de lotes y upload de páginas

## Resumen

Sesión centrada en ampliar la API web SaaS con dos piezas nuevas sobre la base
publicada el 2026-04-07: el CRUD completo de lotes (`batches`) y el sistema de
subida y gestión de páginas con storage en disco. Todo el trabajo se ha hecho
sobre la rama `feature/web`, con tests integrados antes de cada commit.

## 1. CRUD de lotes

Se han añadido los endpoints REST para gestionar los lotes de cada tenant, con
el mismo patrón tenant-scoped que ya se usaba en aplicaciones.

### Endpoints nuevos

| Método | Ruta | Descripción |
|---|---|---|
| `GET` | `/api/batches` | Lista con filtros opcionales `?application_id=` y `?state=` |
| `POST` | `/api/batches` | Crea un lote validando que la aplicación pertenece al tenant |
| `GET` | `/api/batches/{id}` | Obtiene un lote por ID |
| `PATCH` | `/api/batches/{id}` | Actualiza estado, `page_count`, paths, etc. |
| `DELETE` | `/api/batches/{id}` | Elimina en cascada páginas e historial |

### Puntos de control implementados

- **Validación del estado**: en el PATCH, se rechaza con HTTP 422 cualquier
  valor que no esté en `BATCH_STATES` (`created`, `read`, `verified`,
  `ready_to_export`, `exported`, `error_read`, `error_export`).
- **Aislamiento de tenants**: un tenant no puede listar, leer, modificar ni
  crear lotes dentro de aplicaciones que no sean suyas (HTTP 404 en cualquiera
  de esos casos).
- **Filtros combinables**: se pueden usar `application_id` y `state` a la vez.

### Tests

13 tests nuevos cubriendo: crear, listar, filtros por aplicación/estado,
obtener, actualizar (estado válido e inválido), eliminar, aislamiento de
tenants en ambos sentidos y acceso sin autenticación.

Commit: **`3c02af6 feat: batches CRUD — tenant-scoped REST endpoints`**

## 2. Upload de páginas

Se ha construido todo el flujo para que los clientes puedan subir, listar,
descargar y eliminar páginas dentro de un lote.

### Storage

Se ha creado un servicio de almacenamiento desacoplado del router
(`web/api/storage.py`) basado en filesystem, con una API pensada para migrar
después a MinIO/S3 sin tocar el código de los endpoints.

- **Layout en disco**: `{base_path}/{tenant_id}/{batch_id}/{uuid}.{ext}`
- **Métodos**: `save`, `read`, `delete`, `exists`, `absolute_path`
- **Configuración**: nuevas variables `DOCSCAN_WEB_STORAGE__BASE_PATH`
  (default `/tmp/docscan-storage`) y `DOCSCAN_WEB_STORAGE__PDF_DPI`
  (default 200).

La dependencia se inyecta via `StorageDep` (Depends estándar de FastAPI),
lo que permite sobreescribirla en tests con `tmp_path`.

### Endpoints nuevos

| Método | Ruta | Descripción |
|---|---|---|
| `POST` | `/api/batches/{batch_id}/pages` | Upload multipart de uno o más ficheros |
| `GET` | `/api/batches/{batch_id}/pages` | Lista las páginas del lote |
| `GET` | `/api/batches/{batch_id}/pages/{page_id}` | Metadatos de una página |
| `GET` | `/api/batches/{batch_id}/pages/{page_id}/image` | Descarga del binario |
| `DELETE` | `/api/batches/{batch_id}/pages/{page_id}` | Borra página + fichero |

### Lógica del upload

- **Imágenes individuales** (`jpg`, `jpeg`, `png`, `bmp`, `tif`, `tiff`): se
  guardan tal cual, sin decodificar, creando una `Page` por cada fichero.
- **PDFs**: se abren con `pymupdf` desde memoria, cada página se renderiza a
  PNG (a la DPI configurada) y se guarda como una `Page` independiente. Un PDF
  de 10 páginas → 10 registros `Page` con `page_index` consecutivos.
- **`page_index` incremental**: se calcula el máximo existente en el lote y se
  empieza a partir del siguiente, permitiendo uploads parciales sin conflictos.
- **`batch.page_count` automático**: se recalcula al subir y al borrar
  páginas.
- **Download vía `FileResponse`**: streaming eficiente sin cargar todo el
  fichero en memoria.
- **Validaciones de entrada**: extensión soportada, fichero no vacío, PDF
  válido (en caso contrario HTTP 400).

### Tests

15 tests nuevos repartidos en dos clases:

- **`TestPagesUpload`** (9 tests): imagen individual, varias imágenes en una
  sola llamada, PDF multipágina, formato no soportado, PDF corrupto, fichero
  vacío, `page_index` incremental en varios uploads, lote inexistente, lote
  de otro tenant.
- **`TestPagesRead`** (6 tests): listado, metadatos, descarga (verificando
  magic bytes del PNG), borrado con actualización de `page_count`, aislamiento
  de tenant, acceso sin autenticación.

Commit: **`043e49d feat: pages upload — multipart endpoints con storage filesystem`**

## Estado actual del proyecto

- **Rama `feature/web`**: 2 commits por delante del remote.
- **Tests totales**: **899 passing** (849 desktop + 50 web), 0 failures.
- **Endpoints disponibles**: auth (register, login, me) + CRUD aplicaciones +
  CRUD lotes + gestión completa de páginas.
- **Swagger UI**: operativo en `http://localhost:8001/docs`.

## Decisiones tomadas

- **Almacenamiento filesystem primero, MinIO después**: para no bloquear el
  avance del resto de funcionalidades, el MVP usa disco local. La abstracción
  `FilesystemStorage` está pensada para reemplazarse por `MinIOStorage` sin
  cambiar los routers.
- **PDF splitting en el upload**: se decidió separar los PDFs en páginas en el
  momento de la subida (no durante la ejecución del pipeline). Mantiene la
  coherencia del modelo `Page = 1 página física`.
- **Rutas anidadas bajo batches**: en lugar de exponer `/api/pages/{id}` como
  top-level, las páginas van bajo su lote. Simplifica el tenant-scoping
  (validas una sola vez por el batch) y deja las URLs más expresivas.

## Próximos pasos (en orden)

1. **Ejecución de pipeline**: background task que envuelve
   `PipelineExecutor`. Pendiente decidir si se usa ARQ+Redis (plan original)
   o `BackgroundTasks` de FastAPI para el MVP.
2. **Docker Compose**: servicio unificado con PostgreSQL + MinIO + Redis + API.
3. **Frontend Vue 3**: estructura base con Vite + Tailwind + autenticación.
4. **Migración storage → MinIO**: una vez el docker-compose esté en marcha.

## Archivos clave

- `web/api/storage.py` — `FilesystemStorage` + dependency
- `web/api/schemas/batch.py` — schemas de lote con validador de estado
- `web/api/schemas/page.py` — schemas de página
- `web/api/routers/batches.py` — CRUD de lotes
- `web/api/routers/pages.py` — upload, download y gestión de páginas
- `web/api/config.py` — añadido `StorageSettings`
- `tests/test_web_api.py` — 50 tests web (+28 en esta sesión)
