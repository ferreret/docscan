# Progreso 2026-03-12

## Resumen

Sesion dedicada a mejoras de UI y revision de calidad de codigo (`/simplify`).

## Cambios realizados

### UI del Batch Manager (simplificacion)
- Eliminado modo supervisor, transiciones de estado, reprocesar errores
- Ahora es solo un visor de historico con filtros y posibilidad de reabrir lotes
- Toolbar simplificado: Abrir lote, Actualizar, Eliminar
- Doble click en lote para abrirlo directamente
- Titulo cambiado a "Historico de Lotes"

### UI del Workbench
- Boton "Cerrar lote" para cerrar sin transferir (transiciona a "verified")
- Guard de worker: no permite cerrar lote si el reconocimiento esta en progreso

### Revision /simplify — 10 correcciones aplicadas
1. `blockSignals` en `set_batches` para evitar senales espurias durante repoblacion
2. Query duplicada de paginas eliminada en `_on_batch_selected` (stats calculados en memoria)
3. `APP_IMAGES_DIR` como constante centralizada en `config/settings.py` (11 ocurrencias reemplazadas)
4. `ThemeManager` almacenado en `__init__` en vez de crear throwaway en cada refresh
5. `_on_open_batch` usa cache `_batch_app_map` en vez de DB query innecesaria
6. Guard duplicado extraido a `_require_selected_batch()`
7. Helper `_qdate_to_datetime()` para eliminar duplicacion
8. Guard muerto `app_id == 0` eliminado
9. Timer no refresca si ventana esta oculta
10. Guard de worker en `_on_close_batch`

## Estado actual
- **Tests**: 493 passing, 0 failing
- **Rama**: feat/ui-improvements (cambios sin commitear)

## Proximos pasos
- Commit y push de los cambios del simplify
- Continuar con mejoras pendientes o paso 22 (tests adicionales)
