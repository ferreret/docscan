# Progreso 2026-04-28 — ronda QA web §§ 10-22 + fix de accesibilidad de dialogs

## Resumen

Sesión dedicada a continuar la auditoría manual del workbench web iniciada
ayer (`feature/web`, secciones 10-22 del guion `docs/qa_web_2026-04-27.md`).
Se cubrieron las 13 secciones restantes con Playwright como motor de prueba
end-to-end, se levantó docker compose contra Postgres + MinIO + Redis para un
entorno realista, y se aprovechó para corregir el primer hallazgo crítico
detectado durante la ronda — la fuga de atajos globales con un dialog modal
abierto. Se cierran 38 entradas en la bitácora del documento de QA.

## 1. Ronda de QA — secciones 10-22

### Lo que se ha verificado

**§ 10 atajos de teclado**: 16 atajos comprobados (zoom 0/+/-/F, navegación
←/→/Shift+→, edición R/M/X/B, lote W/Esc, modal ?/H, modificadores solos,
filtro de foco en `<input>`). `F5`, `T`, `P` y `Delete` se cubrieron
indirectamente en sus secciones específicas.

**§ 11 modal cheatsheet**: `?` y `H` abren, las 4 categorías agrupan los 16
atajos correctamente, `Esc` y click en overlay cierran solo el modal sin
desencadenar el atajo global de cerrar lote.

**§ 12 drag-drop de thumbnails**: `POST /batches/:id/reorder` se dispara con
el orden esperado, persiste tras recarga, y soltar en la misma posición no
genera llamada API.

**§ 13 eventos lifecycle (Fase 4)**: configurando scripts vía API, los 5
eventos (`on_batch_loaded`, `on_page_changed`, `on_navigate_prev/next`,
`on_key_event`) se disparan y los logs se ven en el panel LOG del workbench.
Probado pulsando ←/→ y Ctrl+Z (tecla con modificador no mapeada).

**§ 14 reprocess**: tecla `P` ejecuta `POST /pages/:id/reprocess` síncrono y
devuelve la `PageResponse` actualizada.

**§ 15 pipeline run**: F5 dispara `POST /batches/:id/run` con 202 Accepted;
estado del lote pasa de `created` a `read` cuando el pipeline (vacío)
completa.

**§§ 16-17 transfer + export ZIP**: `T` aceptado con 202 (sin transfer
configurado el handler termina sin cambiar estado); export devuelve un ZIP
válido de 30 MB con `pages/page_NNNN.png` × 4 + `manifest.json`.

**§§ 18-20 configurador**: las 7 tabs (Resumen / General / Pipeline / Imagen
/ Campos / Transferencia / Eventos) cargan; el editor de pipeline ofrece los
4 tipos de step (Barcode v1, ImageOp v2, OCR v3, Script Python v4); el editor
de eventos muestra una plantilla `def on_<event>(app, batch): pass` correcta
con un panel de variables (`app`, `batch`, `log`, `http`, `re`, `json`,
`datetime`, `Path`) y sus tipos.

**§ 21 gestión de equipo**: listado, invitar, revocar, validaciones de
self-deactivate (409 "No puedes desactivar tu propia cuenta") y self-delete
(409 "No puedes eliminar tu propia cuenta") funcionan; invitar email ya
existente devuelve 409 "Ya existe un usuario con ese email".

**§ 22 multi-tenancy (crítico)**: con un token de TestCo se intentaron
operaciones cruzadas contra recursos de QA Company. **Todas devolvieron 404**
sin distinguir "no existe" vs "no es tuyo": GET, POST, PATCH, DELETE en
`/batches/:id`, reorder, run, transfer, export, events, upload de páginas,
PATCH de `/applications/:id`, listado `/api/users`. WebSocket cross-tenant
también rechazado al instante. **APRUEBA**.

### Hallazgos registrados en bitácora (entradas #28-38)

11 entradas nuevas. Sólo 1 corregida en esta sesión (#28); las demás
quedan documentadas con causa raíz y fix sugerido para una próxima
iteración:

| # | Gravedad | Tema |
|---|---|---|
| **28** | **alto (corregido)** | Dialogs sin `role/aria-modal` → atajos globales activos con dialog abierto |
| 29 | UX | `Shift+→` sin needs_review es silente (no toast) |
| 30 | UX | `object-cover` recorta brutalmente las thumbs apaisadas |
| 31 | bajo | Inconsistencia "Insertar barcode" / "+ Añadir" / "Añadir barcode manual" |
| 32 | bajo (doc) | Ejemplos de scripts del propio doc QA están malformados |
| **33** | **medio** | `event_dispatcher` sobreescribe `batch_fields_updated` aunque el script no toque batch.fields |
| **34** | **medio** | `PageContext` expuesto a scripts no incluye `page.id` (solo `page_index`) |
| 35 | bajo | Race en orden de logs `on_page_changed` antes que `on_batch_loaded` |
| 36 | bajo | LogPanel muestra "10:11:38 AM" (12h) y "1 entries" sin pluralización |
| 37 | UX | Reprocesar página borra flags manuales (`needs_review`, etc.) |
| **38** | **medio (seguridad)** | Admin puede degradarse a `operator` sin checks → tenant queda sin admins |

## 2. Fix #28 — dialogs del workbench sin role/aria-modal

**Problema descubierto en QA § 10**. Al pulsar `B`, el `AddBarcodeDialog` abría
con un input "Valor" auto-focuseado, pero si el foco se perdía (blur del
input, click en otro sitio) los atajos globales seguían activos. Probado:
pulsando `R` con foco en `<body>` → la página subyacente rotaba (verificado
con dimensiones del PNG en MinIO: 1240×1754 → 1754×1240). Pulsando `Escape`
escapaba al handler global y cerraba el lote en lugar del dialog. Sólo el
`ShortcutsHelpDialog` estaba protegido (`role="dialog" aria-modal="true"` +
focus management); los demás dialogs del workbench (`AddBarcodeDialog`,
`DeleteBarcodeDialog`) no tenían esa salvaguarda.

### Cambios

Tres ajustes complementarios:

1. **Filtro reforzado en `useWorkbenchShortcuts.isEditableTarget`**: ahora
   consulta `document.querySelector('[role="dialog"][aria-modal="true"]')` y
   bloquea atajos si hay cualquier modal abierto en el documento. El filtro
   deja de depender del target del evento, sobrevive a foco perdido en body.

2. **`AddBarcodeDialog` y `DeleteBarcodeDialog`**: añaden `role="dialog"`,
   `aria-modal="true"`, `aria-label` y `tabindex="-1"` al wrapper interior.
   Focus management con `useTemplateRef` + `nextTick().focus()` en el watcher
   de `visible`, con `immediate: true` para cubrir componentes montados con
   `visible=true` desde el primer frame.

3. **Listener global de Escape** (`document.addEventListener('keydown',
   onDocEsc, true)`) registrado mientras el dialog está abierto, con
   `stopPropagation` para que el handler global no reciba la tecla. Garantiza
   que `Esc` cierre el modal incluso si el target queda en body.

### Tests

4 tests de regresión:

- En `useWorkbenchShortcuts.test.ts`: dialog modal abierto bloquea aunque
  target sea body; `role="dialog"` sin `aria-modal` no bloquea (regression
  safe).
- En `AddBarcodeDialog.test.ts`: el wrapper expone `role="dialog"`,
  `aria-modal="true"`, `aria-label="Añadir barcode manual"`; `Escape` emite
  `close` vía listener global.

**362/362 tests verdes** (358 anteriores + 4 nuevos). Verificado E2E con
Playwright tras el fix: B abre, blur input → foco en body → R no rota la
página, Esc cierra dialog sin cerrar lote.

Commit: **`976fd5f fix(web-frontend): atajos globales activos con dialog de
barcode abierto`**

## Estado actual del proyecto

- **Rama**: `feature/web`, 1 commit por delante de `origin/feature/web`
  (pendiente push).
- **Tests totales**: **1402 passing** (849 desktop + 191 backend web + 362
  frontend web), 0 failures.
- **Doc QA**: `docs/qa_web_2026-04-27.md` ahora con 38 entradas en bitácora,
  cobertura de §§ 1-22 (todas las del guion).
- **Endpoints workbench verificados E2E**: GET/POST/PATCH/DELETE batches,
  pages, reorder, rotate, run, transfer, export, events lifecycle (5),
  reprocess, applications, users, invitations, WebSocket.

## Decisiones tomadas

- **Filtro de atajos basado en presencia de modal, no en target**: la
  solución más simple y coherente con la semántica HTML. `aria-modal="true"`
  bloquea por contrato la interacción con el contenido fuera, así que el
  composable la usa como señal binaria sin importar dónde caiga el foco.

- **`@click.self` en overlay + `tabindex="-1"` con focus en wrapper**:
  duplicación a propósito. El click outside cierra como antes; el focus al
  wrapper garantiza que `Esc` desde input pierdarico llegue al handler. El
  listener global cubre el caso límite de foco perdido en body sin meter un
  focus-trap completo (que sería más invasivo).

- **Reset de password directo en BD para QA**: en lugar de pedir
  credenciales, se resetearon `admin@testco.com` (Test1234!) y
  `admin@otherco.com` (Other1234!) con un `INSERT` en Postgres. Los tokens
  generados con esos hashes son temporales para la sesión de QA.

## Próximos pasos (en orden)

1. **Push `feature/web`** al remote (preferencia: push al cerrar sesión).
2. **Triaje de la bitácora**: priorizar #33 (dispatcher), #34 (page.id en
   eventos) y #38 (degradación de admin) — los 3 de gravedad media.
3. **Reaprovechar el doc QA**: actualizar la § 13 con scripts que sí
   compilan (firma `def on_<event>(...)` + `log.info(...)` + `logs:
   list[dict]`) — entrada #32.
4. **Mejoras UX acumuladas**: Shift+→ silente (#29), thumbs apaisadas
   (#30), formato 12h del LogPanel (#36), aviso al reprocesar (#37).
5. **Fase 5 web**: a discutir con el jefe — el QA cierra el work del MVP de
   workbench. Próximas direcciones: notifications, scheduling de
   transferencias, multi-usuario simultáneo en mismo lote.

## Archivos clave

- `web/frontend/src/composables/useWorkbenchShortcuts.ts` — filtro reforzado
  contra dialogs modales.
- `web/frontend/src/components/workbench/AddBarcodeDialog.vue` — accesibilidad
  + focus management + listener Esc.
- `web/frontend/src/components/workbench/DeleteBarcodeDialog.vue` — idem.
- `web/frontend/tests/composables/useWorkbenchShortcuts.test.ts` — +2 tests.
- `web/frontend/tests/components/workbench/AddBarcodeDialog.test.ts` — +2 tests.
- `docs/qa_web_2026-04-27.md` — bitácora extendida a 38 entradas (11 nuevas
  hoy con causa raíz y fix sugerido).
